public class longestCommonPrefix {
    public static void main(String[] args) {
    String[] strArray = {"Fix", "Fish", "Finish"};
    StringBuilder sb = new StringBuilder();
     for(int i =0; i<strArray.length; i++){
        System.out.println(strArray[i]);
     }
    }
}
